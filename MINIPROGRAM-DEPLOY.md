# 微信小程序上线指南

## 一、本地导入与调试

### 1. 下载微信开发者工具
https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

### 2. 导入项目
- 打开开发者工具 → 「导入项目」
- 项目目录：选择 `/root/repositories/exercise-helper-miniprogram/` 目录（独立项目）
- AppID：先用测试号（或填写你注册的小程序 AppID）
- 项目名称：闻鼓而动
- 后端服务：不使用云服务

### 3. 开发者工具设置
- 「详情 → 本地设置」里勾选「不校验合法域名」（本地调试用）
- 基础库版本设置为 3.3.4 或以上

### 4. 本地调试顺序
1. 编译运行，检查选择页是否正常显示两个卡片
2. 点进晨练/晚练，检查训练页 idle 状态渲染
3. 点「开始训练」，听语音、看倒计时是否正常
4. 点暂停 → 继续 → 退出
5. 测试设置页、记录页导航
6. 点「清空记录」「重置设置」「清除语音缓存」

---

## 二、真机测试（必须）

开发者工具模拟器无法完全模拟真机行为（尤其音频），必须用「真机调试」。

### 真机测试 checklist

| 优先级 | 测试项 | 通过标准 |
|--------|--------|----------|
| P0 | 训练全程语音播报 | 每个动作开始/结束、倒计时数字、休息提示都有声 |
| P0 | 蜂鸣音效 | 阶段结束前 5 秒/1 秒提醒音正常 |
| P0 | 屏幕常亮 | 训练中屏幕不会自动熄屏 |
| P0 | 进度环渲染 | 倒计时圆环正确，颜色渐变显示正常 |
| P0 | 返回键二次确认 | 训练中按返回弹出「确定放弃？」确认框 |
| P1 | 模式切换 | 抗阻日/放松日切换正确，徽章显示正确 |
| P1 | 隔天抗阻判定 | 昨天完整训练后今天自动显示「系统建议：放松」 |
| P1 | TTS 预下载 | 回到选择页后音频自动下载完成，播放无延迟 |
| P1 | 记录保存 | 训练完成后记录页显示本次记录 |
| P1 | 设置修改生效 | 修改参数后重新开始训练生效 |
| P2 | 环境音切换 | beat/dawn/rain 三种环境音正常播放/停止 |
| P2 | 语音 ducking | 开/关 ducking 时环境音音量变化正确 |
| P2 | 清除缓存 | 设置页清除缓存后重新播放走远程 |

### 真机调试步骤
1. 开发者工具点「预览」→ 扫码打开真机调试
2. 打开调试面板（vConsole）
3. 重点关注 Console 是否有红色错误
4. Network 面板确认 mp3 请求成功

---

## 三、小程序后台配置（提审前必须做）

### 1. 注册小程序
https://mp.weixin.qq.com/ 注册小程序账号
- 主体类型：个人或企业均可
- 类目选择：「工具 → 运动健身」或「工具 → 健康」（健身运动类，无需特殊资质）

### 2. 配置服务器域名
进入「开发管理 → 开发设置 → 服务器域名」，添加：

| 域名类型 | 域名 | 说明 |
|----------|------|------|
| downloadFile 合法域名 | `https://exercise.touchren.pub` | TTS 音频下载 |

> 注意：
> - 必须 HTTPS
> - 域名必须 ICP 备案（exercise.touchren.pub 已备案）
> - 不能带端口、路径

### 3. 配置 AppID
拿到 AppID 后，修改 `miniprogram/project.config.json`：
```json
"appid": "你的AppID"
```

### 4. 隐私协议配置（重要）
微信 2023 年 9 月起强制要求隐私协议。进入「设置 → 服务内容声明 → 用户隐私保护指引」配置。

本项目需要声明的隐私项：
- **keepScreenOn**（屏幕常亮）—— 声明「为了训练过程中屏幕不熄屏」
- **downloadFile**（下载文件）—— 声明「为了缓存语音播报音频，减少流量消耗」

不需要声明的：
- 不获取用户信息
- 不获取位置
- 不上传任何数据
- 不使用相册/摄像头/麦克风

### 5. 补充材料
- 隐私政策 URL：可以放在 PWA 站点上（exercise.touchren.pub/privacy.html）
- 测试账号：无需（无登录）
- App 介绍截图：5 张 750×1334 截图（选择页、训练页、训练中、设置页、记录页）

---

## 四、提审发布

### 1. 上传代码
开发者工具点「上传」，填写版本号和项目备注。

### 2. 提交审核
小程序后台「版本管理 → 开发版本」→ 提交审核。

审核备注建议写：
> 纯本地运动训练辅助工具，全程语音引导健身训练，无账号无登录无数据上传。功能包括：晨练/晚练两套训练方案，语音播报动作名称和节奏，本地训练打卡记录。

### 3. 审核注意事项
- 首页不能是空白页（已确保）
- 不要出现「医疗」「治疗」「治愈」等医疗术语（已用「抗阻」「拉伸」「训练」表述）
- 涉及健康类类目，不要声称有治疗效果
- 隐私弹窗必须正常弹出（开发者工具里测试）

### 4. 发布
审核通过后，在「版本管理」点「发布」即可上线。

---

## 五、版本更新策略

PWA 版本用 SW 缓存 + 版本号控制。小程序版本更新机制：

- **开发版/体验版**：每次上传后重新扫码即更新
- **正式版**：微信自动在后台检查更新，下次冷启动时应用新版本
- **热更新**：小程序官方自动处理，无需手动写更新逻辑

如果需要强制更新提示，可以在 `app.js` 的 `onLaunch` 里加：
```js
const updateManager = wx.getUpdateManager();
updateManager.onUpdateReady(() => {
  wx.showModal({
    title: '更新提示',
    content: '新版本已准备好，是否重启应用？',
    success(res) {
      if (res.confirm) updateManager.applyUpdate();
    }
  });
});
```

---

## 六、代码结构速查

```
miniprogram/
├── app.js / app.json / app.wxss      # 小程序入口
├── project.config.json                # 开发者工具配置（改 AppID 在这里）
├── sitemap.json                       # 搜索配置
├── pages/
│   ├── selector/                      # 首页：选择晨练/晚练
│   ├── training/                      # 训练页：核心计时+语音
│   ├── settings/                      # 设置页
│   └── records/                       # 打卡记录页
├── utils/
│   ├── constants.js                   # 常量（TTS URL、存储前缀）
│   ├── storage.js                     # 存储封装
│   ├── tts-downloader.js              # TTS 音频预下载
│   ├── workout-data-morning.js        # 晨练动作定义
│   ├── workout-data-evening.js        # 晚练动作定义
│   ├── engine.js                      # 训练状态机
│   └── audio.js                       # TTS + 音效 + 环境音
└── components/
    └── progress-ring/                 # Canvas 进度环组件
```
